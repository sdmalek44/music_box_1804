<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <p> <%= link_to "Add New Artist", new_artist_path, id: "artist-link" %></p>

    <% @artists.each do |artist| %>
    <p><%= link_to artist.name, artist_path(artist) %></p>
    <% end %>
  </body>
</html>
